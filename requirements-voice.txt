# Voice features for BEASTT (optional).
# Install with:  pip install -r requirements-voice.txt
#
# Text-to-speech (BEASTT talks back) -- fully offline, zero config.
pyttsx3>=2.90

# Speech-to-text (BEASTT listens to you) -- offline via Whisper.
# We use `sounddevice` for mic capture instead of PyAudio: it bundles its own
# PortAudio engine, so there's no C++ compiler needed (works on Windows +
# Python 3.14 out of the box).
sounddevice>=0.4.6
openai-whisper>=20231117
numpy>=1.24
